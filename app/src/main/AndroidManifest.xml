<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kishore.workshopform">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".OneParticipant"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".Twoparticipant"
            android:label="@string/title_activity_two_participant"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ThreeParticipant"
            android:label="@string/title_activity_three__participant"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FourParticipant"
            android:label="@string/title_activity_fout__participant"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
